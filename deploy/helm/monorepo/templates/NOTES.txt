Thank you for installing {{ .Chart.Name }}!

Release: {{ .Release.Name }}
Namespace: {{ .Release.Namespace }}

--------------------------------------------------------------------
Services
--------------------------------------------------------------------
{{- range $name, $svc := .Values.services }}
{{- if eq $name "auth" }}

Auth service is running inside the cluster.

{{- if eq ($svc.service.type | default "ClusterIP") "NodePort" }}

Access auth via NodePort:

  http://localhost:{{ $svc.service.nodePorts.http }}

{{- else }}

Access auth via port-forward:

  kubectl port-forward svc/{{ include "monorepo.fullname" $ }}-auth 8080:{{ $svc.service.httpPort | default 80 }}

Then open:

  http://localhost:8080

{{- end }}

{{- end }}
{{- end }}

--------------------------------------------------------------------
Internal services
--------------------------------------------------------------------
Internal services (auth/user gRPC or HTTP) are accessible via
Kubernetes DNS inside the cluster only, for example:

  auth:{{ .Values.services.auth.service.grpcPort | default 9090 }}
  user:{{ .Values.services.user.service.grpcPort | default 9090 }}

--------------------------------------------------------------------
Configuration
--------------------------------------------------------------------
To inspect values used for this release:

  helm get values {{ .Release.Name }}

To upgrade with new values:

  helm upgrade {{ .Release.Name }} . -f values.yaml

--------------------------------------------------------------------
